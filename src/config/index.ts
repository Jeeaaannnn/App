// Core packages

// Third party packages

// Custom packages

/**
 * Script start
 */
export default {
  dataRefreshInterval: 60 * 1000, // The seconds between automatic data refresh,
  requestsTimeout: 10 * 1000, // General requests timeout in seconds
};
