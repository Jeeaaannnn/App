export default interface DeviceInfo {
  fontScale: number;
  model: string;
  deviceId: string;
  systemName: string;
  uniqueId: string;
  systemVersion: string;
  brand: string;
  appVersion: string;
  buildNumber: string;
}
